Before using the R-Script please download the "lubridate" packages if not installed yet.

You can use the following link to download the compressed file "lubridate_VERSION.tar.gz" and install it afterwards:

https://cran.r-project.org/web/packages/lubridate/index.html

Or use the opportunity to install it over RStudio: 

Packages - Install - Repository CRAN -Packages: lubridate
For example:
install.packages("lubridate")